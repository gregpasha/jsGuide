// javascript is lexically scoped, which means that
// variables have the scope from when they are defined (as opposed to invoked)

// javascript has function scope

// javascript does not have block scope